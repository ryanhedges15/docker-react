sudo: required
services:
  - docker

before_install:
  - docker build -t ryanhedges15/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true ryanhedges15/docker-react npm run test -- --coverage
language: generic
